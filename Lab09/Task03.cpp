/*
#include <iostream>
using namespace std;

char uppercase(char ch) {
    if (ch >= 'a' && ch <= 'z') {
        return ch - 'a' + 'A';
    }
    return ch;
}

int main() {
    char lowercase;
    cout << "Enter a lowercase alphabet: ";
    cin >> lowercase;
    char uppercaseChar = uppercase(lowercase);
    cout << "Uppercase: " << uppercaseChar << endl;
    return 0;
}
*/