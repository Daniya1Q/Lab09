/*
#include <iostream>
using namespace std;

void compareDistances(int feet1, float inches1, int feet2, float inches2) {
    float totalInches1 = feet1 * 12 + inches1;
    float totalInches2 = feet2 * 12 + inches2;

    if (totalInches1 > totalInches2) {
        cout << "The first distance is larger." << endl;
    }
    else if (totalInches1 < totalInches2) {
        cout << "The second distance is larger." << endl;
    }
    else {
        cout << "Both distances are equal." << endl;
    }
}

int main() {
    int feet1, feet2;
    float inches1, inches2;

    cout << "Enter the first distance in feet and inches:" << endl;
    cout << "Feet: ";
    cin >> feet1;
    cout << "Inches: ";
    cin >> inches1;

    cout << "Enter the second distance in feet and inches:" << endl;
    cout << "Feet: ";
    cin >> feet2;
    cout << "Inches: ";
    cin >> inches2;

    compareDistances(feet1, inches1, feet2, inches2);

    return 0;
}
*/